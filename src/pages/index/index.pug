extends /src/layout/landing

block content
  for group, i in pages
    +section({
      title: `${group.section.title} (${group.items.length})`,
      mods: ["border"]
    })
      +cards-slider({
        items: group.items,
        index: i
      })
